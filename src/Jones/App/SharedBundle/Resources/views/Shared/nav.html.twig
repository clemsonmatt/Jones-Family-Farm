<nav class="navbar navbar-custom1 navbar-fixed-top" style="height: 50px; border: none; {% if hide_display|default(false) %}display: none; {% endif %}">
    <div class="container-fluid container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/" style="max-width: 8%;">
                <img src="{{ asset('bundles/jonesappshared/images/BeeWhite.png') }}" style="max-width: 100%; margin-top: -8px;">
            </a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse pull-right" id="bs-example-navbar-collapse-1" style="font-size: 1.4em;">
            <div style="margin-top: 10px;">
                <a href="#" class="btn btn-custom-clear btn-sm pull-right">
                    <i class="fa fa-sign-in"></i> Login
                </a>
                <a href="{{ path('jones_app_shared_cart_index') }}" class="pull-right" style="margin-right: 15px; color: #fff;">
                    <i class="fa fa-shopping-cart"></i>
                    <span class="badge">3</span>
                </a>
            </div>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
            <ul class="nav navbar-nav">
                <li>
                    <a href="{{ path('jones_app_shared_default_index') }}" class="{{ active_tab.home }}">
                        Home
                    </a>
                </li>
                <li>
                    <a href="{{ path('jones_app_shared_product_index') }}" class="{{ active_tab.product }}">
                        Our Products
                    </a>
                </li>
                <li>
                    <a href="#" class="{{ active_tab.contact }}">
                        Contact Us
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
